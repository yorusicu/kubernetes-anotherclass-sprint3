apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shared-volume-3231
  namespace: anotherclass-323
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1G
  storageClassName: ""  // Storage 사용 안함 설정
  selector:
    matchLabels:
      pv: shared-volume-3231